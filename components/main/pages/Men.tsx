import Shortcut from "../shortcuts/Shortcut";

const Men = () => {
    return (
        <div>
            <Shortcut title={undefined} subTitle={undefined} theme={"topList"}/>
        </div>
    );
};
export default Men;